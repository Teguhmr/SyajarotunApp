<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teguh.sejarahislam">

    <application
        android:name=".GameApplication"
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="portrait"
        android:configChanges="orientation|keyboardHidden"
        android:theme="@style/AppTheme">
        <activity android:name=".Buku2Activity"
            android:screenOrientation="landscape"></activity>
        <activity
            android:name=".BukuActivity"
            android:screenOrientation="landscape"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".services.BackgroundMusicService" />
        <service android:name=".services.BackgroundMusicBookService" />
        <service android:name=".services.BackgroundMusicGamesService" />

    </application>

</manifest>